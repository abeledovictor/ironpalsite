{
  "scripts": {
    "deploy": "gatsby build --prefix-paths && cp CNAME public && cp favicon.ico public && gh-pages -d public"
  },
  "dependencies": {
    "@narative/gatsby-theme-novela": "^0.18.4",
    "gatsby": "^2.27.4",
    "gatsby-plugin-google-gtag": "^2.5.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "gatsby-plugin-root-import": "^2.0.5",
    "gh-pages": "^3.1.0"
  }
}
